from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a regression task, predict the points depend on some text and non-text features with several approches',
    author='Tarek yahia',
    license='MIT',
)
